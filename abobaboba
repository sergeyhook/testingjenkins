FROM docker:dind

USER root

RUN apk update

RUN apk add --no-cache openjdk8
RUN apk add --no-cache gradle
RUN mkdir -p ~/.gradle/native
RUN chmod -R 777 ~/.gradle/native
WORKDIR /home/gradle/project

COPY . .

